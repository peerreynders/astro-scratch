---
// file: src/components/page-header.astro

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="c-page-header">
	<div class="u-wrapper u-flow">
		<!-- prettier-ignore -->
		<h1>{title}</h1>
		{
			Astro.slots.has('default') && (
				<div class="u-measure-long">
					<slot />
				</div>
			)
		}
	</div>
</div>
